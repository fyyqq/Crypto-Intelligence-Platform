services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: crypto
      POSTGRES_PASSWORD: crypto
      POSTGRES_DB: crypto_intelligence
    ports:
      - "5432:5432"
    volumes:
      - crypto_pg_data:/var/lib/postgresql/data

volumes:
  crypto_pg_data:

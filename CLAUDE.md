# Claude Code Project Rules

## 🤖 Handoff & Session Limit Rule
* **Mandatory Action:** AFTER completing any significant change, sub-task, or bug fix, you MUST immediately rewrite and update the sections below (`### 🔧 Current State` and `### ➡️ Next Steps`) inside this file.
* **Why:** This ensures that if the terminal session suddenly runs out of API limits, the current progress state is perfectly preserved right here, allowing a seamless handoff to GitHub Copilot inside VS Code without losing data.


---

## Playwright Testing Rules

When asked to run, write, or execute Playwright tests against web pages loaded in the VS Code context:
- **Never open external Chrome windows or tabs.** All browser actions must be completely hidden.
- **Utilize the Context Picker Panel:** Use the DOM structure, selectors, and context provided by the active VS Code Context Picker panel for selecting elements.
- **Headless Execution Only:** Execute all test runs or interactions headlessly in the background. Use either:
  1. The installed Playwright MCP server tools (`playwright_navigate`, `playwright_click`, etc.) which run entirely in the background.
  2. The integrated terminal via `npx playwright test --headless`.
- **Provide Results in Chat:** Print the execution outcome, assertion results, or console errors directly back into the chat interface without creating visual browser popups.

---

## Commit & Push Rule
* **Mandatory Action:** After completing and verifying any change, sub-task, bug fix, or feature — not batched until end of session — immediately `git add`/`git commit`/`git push` to `origin/main` (a normal local commit + push, not the GitHub MCP `push_files` workaround, unless local git is unavailable).
* **Why:** Keeps `origin/main` current for handoff to GitHub Copilot or another session at any moment, and avoids losing verified work if the session ends unexpectedly.
* **How to apply:** Commit message should summarize the "why" of the change in 1-2 sentences. Update the `### 🔧 Current State` / `### ➡️ Next Steps` sections (per the Handoff rule above) in the same commit when relevant.

---


## Crypto Intelligence Platform — Session State Summary

### ✅ Feature 1: Dynamic Market Data Sync & Dashboard — COMPLETE
**Backend Implementation:**
- **Sync architecture** (rule 4 in `ai-instructions.md`): Three-tier sync strategy optimized for cost and freshness:
  - **24h full-universe sync** (`app/scheduler/jobs.py::run_market_data_sync`): All CMC-listed coins, categories, and per-coin contract data — runs once daily to detect new listings while protecting free-tier credits.
  - **1h hot sync** (`run_hot_listings_sync`): Top 500 coins' quotes (price, mcap, volume, % changes) via single API call — cheap/hourly.
  - **60s view-driven live sync** (`frontend/frontend/state/coin_state.py::live_sync_loop` / `sync_visible_page`): Only coins currently on-screen, refreshes every minute + immediately on filter/pagination changes. Cross-imports `app.services.market_data_service.MarketDataService.sync_ids` for minimal API footprint.
  - All three scheduled in `start_scheduler()`.

**Frontend (Responsive Design 320px–1728px+):**
- **Data table**: dark-mode, sortable columns, market-cap-ranked, 24h/7d trend sparkline cells. All 10 columns always visible; horizontal scroll (`.coin-table-scroll-fix` overflow-x) is fallback when table width exceeds available space.
- **Sticky header**: position:sticky header cells with `height: 100%` to prevent gaps when labels wrap across rows.
- **Pagination**: 50/100/200/500 rows per page, page-window arrow controls. Responsive padding adjusts for mobile.
- **Dynamic pill filters**: Top-10-by-count narrative pills + top-20-by-count chain pills (AND-combined), computed fresh on each sync (not hardcoded). Wrapped layout in sidebar (no slider at tablet/mobile widths). `CoinState.categories` / `CoinState.chains`.
- **Real-time search**: Name/ticker search queries all coins (not just current page), debounced 300ms. Desktop: icon-triggered popover. Mobile (<768px): always-visible full-width field in its own row below heading.
- **Skeleton loading**: Genuinely visible (~150–250ms) on every filter/search/pagination change via async gate (`asyncio.sleep(0.25)` in state handlers).
- **Alert & news sliders**: Horizontal drag-scroll with arrow buttons at all viewport widths. Alert card badges wrap to prevent overlap on narrow widths.
- **Footer**: Centered/stacked on mobile/tablet (<1280px), left-aligned two-column layout on desktop (≥1280px).
- **Table surface styling**: Border/background/backdrop-blur stripped below 1280px (tablet/mobile), preserved on desktop for visual hierarchy.

**UI Polish & Fixes (2026-09-24 session):**
- Header white gap fix: Added `height: 100%` to sticky header cells to restore auto-stretch behavior when wrapped labels make rows taller than single-line cells.
- Table surface variant cleanup: Removed Radix "surface" border/backdrop-blur on mobile/tablet, keeping card look only at desktop widths.
- Footer centering extended: Navbar and copyright row stay centered through 1279px, only split to desktop layout at 1280px+.
- Mobile search bar redesign: Magnifying glass icon hidden <768px, replaced with always-visible full-width field in dedicated row.
- Alert card badge wrap: Added `flex-wrap: wrap` to prevent "REAL-WORLD ASSETS (RWA)" + "ONDO" overflow; widened narrowest card breakpoints (270px/290px) for breathing room.
- Search bar spacing: Added top margin to mobile search field for visual separation from header row.

**Verified 2026-09-24:**
- Live QA pass: narrative pill filtering, chain pill filtering, rows-per-page toggle, pagination arrows, search (open/type/clear), responsive layout at 6+ viewport widths (320/375/480/768/1024/1280/1366/1440/1728px).
- Zero console errors, zero failed network requests, zero page-level horizontal overflow at any width.

### ⏳ Feature 2: Knowledge Base & AI Business Model Agent — IN PROGRESS
**Backend Implementation:**
- **AI business summary** (`app/services/business_summary_service.py::get_business_summary`): On-demand OpenRouter call (`nvidia/nemotron-3-ultra-550b-a55b:free`, NOT Claude 3.5 Sonnet as originally scoped in `ai-instructions.md` — that model is retired from OpenRouter, and this project's OpenRouter account is $0-balance free-tier, which 402s on any paid model regardless of price; see `settings.openrouter_model` comment). Prompts for a leading `CATEGORY: <label>` line (parsed via `_CATEGORY_LINE` regex, e.g. "Tokenized Securities Lending Protocol") plus 2-3 `TITLE: <heading>` paragraphs. Gated by `Coin.business_summary_updated_at` + `settings.business_summary_ttl_days` (60d default) — periodic, not one-time, since a project's real business model can drift.
- **About-section real-description upgrade, two independent one-time steps** (`frontend/frontend/state/coin_state.py::_fetch_better_description`, called from `CoinState.refresh_coin_description`):
  1. `app/services/coingecko_service.py::upgrade_description` — CoinGecko `/coins/{platform_id}/contract/{address}` contract-address lookup for a real curated project write-up, replacing CMC's auto-generated "`<name>` is a cryptocurrency and operates on..." boilerplate (detected via `is_boilerplate_description`'s 3-marker fingerprint). Gated once-ever by `Coin.description_synced_at`.
  2. `app/services/description_ai_service.py::generate_description_from_sources` — if STILL boilerplate after step 1 (typical for memecoins/newly-listed tokens with no whitepaper anywhere, e.g. Dogecoin, STONK, Pons confirmed live), generates a grounded AI paragraph from the coin's own website text (`_fetch_website_text`, HTML-stripped via `_TAG_STRIP_RE`/`_ANY_TAG_RE`, capped at `_MAX_WEBSITE_CHARS = 4000`) and/or up to `_MAX_TWEETS = 8` already-cached X posts (`coin.cached_tweets`, reused from `social_service.py`, not re-scraped). System prompt explicitly forbids inventing business model/tokenomics/team details not in the source text. Gated once-ever by `Coin.description_ai_generated_at` (independent of `description_synced_at`).
- **Real CEX/DEX market pairs** (`app/services/market_pairs_service.py::get_market_pairs`): CMC's own `/v2/cryptocurrency/market-pairs/latest` 403s on this project's Basic/free CMC key (error_code 1006 — needs paid tier), so this uses CoinGecko's free `/coins/{id}?tickers=true` instead, reusing `coingecko_service`'s contract-address / top-500-symbol-map coin-id resolution.
  - CEX rows: filtered to `_CEX_ALLOWED_NAMES` (CMC's real top-15 spot-exchange ranking from coinmarketcap.com/rankings/exchanges/, plus "Binance Alpha" per explicit request — flagged as a no-op today since CoinGecko's own `/exchanges/list` has no such distinct tracked entity). USDC-quoted pairs dropped via `_EXCLUDED_CEX_QUOTES` whenever a non-USDC pair exists from the same exchange pool, falling back to keeping USDC only if that would empty the tab.
  - DEX rows: filtered to `_DEX_ALLOWED_BY_CHAIN[chain_name]` — top-15-per-chain real rankings from coingecko.com/en/exchanges/decentralized/`<chain>`, individually curated for Solana/Ethereum/BNB Smart Chain (BEP20)/Base/Arbitrum/Polygon (keyed by `CoinContract.platform_name`, same spelling as `coingecko_service._CHAIN_TO_COINGECKO_PLATFORM`); any other chain falls back to `_DEX_ALLOWED_FALLBACK` (CoinGecko's cross-chain top-15). `_NATIVE_ASSET_CHAINS` maps a native/root asset (ETH, SOL, BNB, MATIC/POL, AVAX — no `CoinContract` row of its own) to its own chain's ranking.
  - Both sides capped to 15 rows (`[:15]`), sorted by `volume_24h` desc, never padded if fewer real listings exist. Gated by `Coin.market_pairs_updated_at` + `settings.market_pairs_cache_ttl_hours` (1h default — far shorter than the description/summary TTLs since exchange volume goes stale fast).
- **New Postgres columns** (`app/models/coin.py`, migration `b1d0fd86c529`): `business_summary`, `business_summary_updated_at`, `business_summary_model`, `business_model_category`, `description_ai_generated_at`, `cached_market_pairs`, `market_pairs_updated_at`.
- **No scheduled batch worker yet** — every step above is on-demand, triggered only by a page visit (`frontend/frontend/frontend.py`'s `/coin/[symbol]` `on_load` list: `CoinState.refresh_coin_description`, `refresh_business_summary`, `refresh_market_pairs`, each a `@rx.event(background=True)`). This deviates from `ai-instructions.md`'s literal Feature 2 spec ("background worker to fetch whitepapers/documentation for the Top 500 cached coins") — nothing in `app/scheduler/jobs.py` proactively generates these yet, so an unvisited coin has no AI summary/description upgrade until someone opens its page.

**Frontend (inline in the existing coin_detail page — no new side-drawer/modal; also a deviation from `ai-instructions.md`'s literal spec, which asked for a modal/drawer triggered on coin click):**
- **Category badge** (`frontend/frontend/components/coin_detail.py::_chart_column`): next to the "`<Coin>` Live Chart" heading, `justify="between"` row. Reads `coin["category_badge_display"]` / `coin["has_category_badge_display"]` (computed in `CoinState._build_row` and patched live in `refresh_business_summary`) — falls back to the coin's real CMC `primary_narrative` tag when no AI category has been generated yet (confirmed live: Ethereum shows "Layer 1"), so every coin's page shows *some* badge instead of an empty gap. Style: `variant="surface"`, `radius="full"`, `style={"border": "1px solid royalblue", "color": "white", "background": "royalblue"}` — solid, no pulse animation (glow removed per explicit request).
- **About section** (`_about_section`): unchanged structural home for both the CoinGecko-upgraded and AI-inferred descriptions — `coin["description_tokens"]` (keyword-highlighted via `_render_highlight_token`/`_tokenize_highlights` in `coin_state.py`), fixed `_ABOUT_COLLAPSED_HEIGHT = "320px"` collapse/expand via `CoinState.about_expanded` + `toggle_about_expanded`, auto-hides the expand arrow for short text via `_about_collapse_fix_script`.
- **AI Summarizations section** (`_business_summary_section`): heading row (`rx.heading("AI Summarizations")` + sparkles icon) matching `_about_section`'s style, per-paragraph blocks via `_business_summary_section_block` (optional `TITLE:` heading + `_highlighted_paragraph(..., color="white")`), bottom credit row (`"Generated by AI"` left, brand-icon + model-name badge right via `_ai_provider_icon`/`CoinState._format_model_badge`). Card style: `border="2px solid royalblue"`, reduced `box_shadow="0 0 10px 1px var(--indigo-a4)"` (glow removed per explicit request, border does the visual work now).
- **Markets section** (`_market_pairs_section` / `_market_pairs_filter_button` / `_market_pair_row`): CEX/DEX-only tabs (`_market_pairs_filter_button("CEX", "cex")`, `("DEX", "dex")` — no "All" tab, removed per explicit request), driven by `CoinState.market_pairs_filter` (default `"cex"`) and `CoinState.filtered_market_pairs` computed var. Table columns: `#`, `Exchange` (icon + name + CEX/DEX `rx.badge`), `Pair`, `Price`, `24h Volume`, `Volume %`, `Last Updated`. Lives inside the same responsive middle chart column as the rest of `_chart_column()` — no new breakpoints; reuses `_CHART_HEIGHTS = ["420px", "480px", "520px", "560px", "600px"]`'s column and `rx.breakpoints(initial="column", lg="row")` from `coin_detail_page()`.
- **Dynamic page title** (`CoinState.page_title` computed var, wired into `frontend/frontend/frontend.py`'s `app.add_page(coin_detail, ..., title=CoinState.page_title)`): browser tab / meta title is `"Repace — <Coin Name>"` (e.g. "Repace — Fartcoin", "Repace — Dogecoin") — the coin's real name, never its ticker, per explicit request. Falls back to `"Repace — Coin Detail"` before `all_coins` has loaded.

**Verified live 2026-09-26 (Playwright, zero console errors on every check):**
- Category badge fallback: Ethereum ("Layer 1"), Ondo (real AI category "Tokenized Securities Lending Protocol").
- About-section AI fallback: Dogecoin (dogecoin.com-sourced 4-sentence real description replacing CMC boilerplate), STONK/stonkfun.xyz (real onchain-coin-platform description). Pons correctly left untouched (declared website domain is dead/NXDOMAIN, zero cached X posts — nothing to ground a description in; attempt still marked so it isn't retried forever).
- Markets top-15 + USDC exclusion: Fartcoin's CEX table dropped Kraken/KuCoin/MEXC's USDC rows in favor of those same exchanges' real USDT/USD/EUR pairs; Fartcoin's DEX table shows a full 15 real Solana-DEX rows; Tether's CEX table dropped BTCC entirely (not in CMC's real top-15) in favor of MEXC/Bybit/OKX/Gate/HTX.
- Dynamic title: confirmed "Repace — Fartcoin", "Repace — Dogecoin", "Repace — STONK (stonkfun.xyz)" on their respective pages.

### 🔧 Bug fix: TradingView chart reload / timeframe reset (2026-09-26 session)
User-reported: the coin-detail page's TradingView chart kept reloading on its own, and the interval kept snapping back to a hardcoded weekly ("W") candle view — any timeframe the viewer picked manually didn't stick.

**Root cause, confirmed live with a MutationObserver** (`frontend/frontend/components/coin_detail.py::_chart_column`): the chart iframe was built via `rx.html(f'<iframe src="{...}">...')` — a raw HTML string handed to React's `dangerouslySetInnerHTML`, not a real typed component. `CoinState`'s single React context re-renders every consumer whenever *any* field changes, and `all_coins` gets reassigned roughly every 60s by `detail_sync_loop` (plus once each by the four on-load background refreshers). Confirmed empirically (childList mutations logged every ~60–62s, `added:1, removed:1` on the iframe node) that this tore down and rebuilt the iframe on every one of those updates — even when the resulting HTML string was byte-for-byte identical — because `dangerouslySetInnerHTML` reassignment vs. a real DOM-diffed prop doesn't get the same "skip if unchanged" treatment here. Each rebuild reset TradingView's own in-widget state (the viewer's manually chosen interval) back to whatever was hardcoded.

**Fix**, both in `coin_detail.py` and `coin_state.py`:
1. `_chart_column()`: replaced the `rx.html(f'<iframe ...>')` string with a real `rx.el.iframe(src=rx.color_mode_cond(...), custom_attrs={"allowtransparency": "true", "frameborder": "0"}, style={...})` — a genuine typed React prop. The surrounding component can still re-render on every state update; React's reconciler now only touches the `src` attribute when its value actually changes, so the iframe (and the viewer's chosen timeframe) survives background syncs.
2. `CoinState._tradingview_iframe_src`: symbol is now read from `self.symbol` (the static `/coin/[symbol]` route param) instead of `self.selected_coin.get("symbol")`, removing an unnecessary dependency on `all_coins` at the value-computation level too (belt-and-suspenders with fix #1).
3. Default `interval` changed from `"W"` (weekly) to `"60"` (1 hour) per explicit request, `range="ALL"` kept so the chart still opens fully zoomed out to the coin's whole listing history on every page load/reload — just at hourly candle granularity instead of weekly. Viewers can still change the interval afterward via TradingView's own UI, and — per fix #1 — it now actually stays changed.

**Verified live (Playwright + MutationObserver + performance/network APIs, Bitcoin's page)**: before the fix, the iframe's parent node saw a childList mutation (remove+re-add) roughly every 60–62s, matching `detail_sync_loop`'s cadence. After the fix, across a 75-second window spanning a full sync tick, zero mutations, zero additional `widgetembed` network requests, and `iframe.addEventListener('load', ...)` fired zero additional times — same DOM node throughout (`iframe === <original reference>` stayed `true`).

### 🔧 Bug fix: TradingView chart wrong/missing symbol + sub-$1 price truncation (2026-09-26 session)
User-reported: several coin pages' chart showed TradingView's "This symbol doesn't exist" error instead of a real chart (e.g. `/coin/zkml`, and intermittently `/coin/btw`, `/coin/shib`); the chart's timeframe also looked "locked" to a weekly/monthly view regardless of the app's own hourly default. Separately, coins priced under a cent (e.g. SHIB, real price `$0.000005887`) showed a flat `$0.00` everywhere a price is displayed.

**Root causes found (four distinct bugs, all in the same chart-symbol code path):**
1. **`range="ALL"` silently overrides `interval`** — confirmed live against TradingView's own widgetembed: passing both `range="ALL"` and `interval="60"` makes TradingView pick its own coarser candle size (month) for the full-history zoom, ignoring the requested interval entirely. This was the real cause of the "locked timeframe" symptom, not anything cache/state-related.
2. **Bare, unprefixed `"{SYMBOL}USDT"` guessing is unreliable** — TradingView doesn't reliably resolve an unprefixed symbol to the right venue for smaller-but-real listings (MEXC/Gate/HTX/...), and worse, can silently latch onto a completely different, malformed symbol (confirmed live: a bare `SHIBUSDT` query resolved to `KUCOIN:SHIBA INUUSDT`, which doesn't exist).
3. **CoinGecko's own per-ticker `base` field can be the coin's full name, not its ticker** — confirmed live for Shiba Inu's own KuCoin listing (`base: "SHIBA INU"`, not `"SHIB"`). `market_pairs_service.py`'s `_display_symbol` was also applying its DEX-only "raw base is a contract address, use the CoinGecko id instead" correction to CEX pairs too, compounding this.
4. **`_fmt_usd` always rounded to 2 decimals** — anything under a cent rounds straight to `$0.00`, hiding the coin's real price entirely.

**Fixes, across `app/services/market_pairs_service.py`, `frontend/frontend/state/coin_state.py`, `frontend/frontend/components/coin_detail.py`:**
1. Dropped the `range` param entirely — `interval="60"` (1h) is now actually honored on every load.
2. New `CoinState._resolve_tradingview_symbol()`: picks a real, exchange-prefixed symbol (e.g. `BITGET:BTWUSDT`) from this coin's own already-fetched CEX market pairs (`_TRADINGVIEW_EXCHANGE_PREFIXES` maps `market_pairs_service`'s real exchange names to TradingView's own prefixes), ranked by that pair's real 24h volume. Returns `None` — never a bare guess — when no real CEX pair resolves.
3. The symbol's **base** always comes from the coin's own known ticker (`self.symbol`), not the pair's own `market_pair` base string (fixes the "SHIBA INU" case structurally, not just for that one coin); the **quote** is validated against a plain `[A-Z0-9]{2,10}` pattern before use.
4. `market_pairs_service._display_symbol`/`_normalize` now only apply the CoinGecko-id-derived name substitution to DEX pairs (`is_dex=True`); CEX pairs always use their own raw ticker.
5. New three-state chart UI (`_chart_column`/`_chart_placeholder` in `coin_detail.py`, `has_tradingview_chart`/`tradingview_chart_pending` in `coin_state.py`): real iframe when a symbol resolves, a "Loading chart…" placeholder while this coin's market-pairs fetch is still in flight, a plain "No live chart available for this coin" message once it's confirmed (via the new `market_pairs_fetched` row flag) that this coin genuinely has no real exchange listing (e.g. DEX-only microcaps like zKML) — never TradingView's own broken-looking error card.
6. Found and fixed a **separate, pre-existing cache-sync bug** while wiring up `market_pairs_fetched`: `CoinState._fetch_market_pairs`'s reflex-sqlite mirror wrote `cached_market_pairs` back but never `market_pairs_updated_at`, so the Reflex-side "has this been fetched" check was permanently `False` even long after Postgres had a real cached result. Now mirrors both fields.
7. `_fmt_usd` (shared by the big price header, homepage table, and Markets pair rows) now shows up to 8 decimals for sub-$1 prices instead of a fixed 2, e.g. SHIB now shows `$0.0000059` instead of `$0.00`. $1+ prices are unaffected.
8. Markets section CEX/DEX tabs: new `CoinState.effective_market_pairs_filter` auto-switches to whichever side (CEX/DEX) actually has real listings when the other is empty — a DEX-only coin like zKML now opens on the DEX tab instead of an empty CEX table.

**Verified live (Playwright, multiple coins after each fix + full server restart to reload backend Python)**: BTW now resolves to `BITGET:BTWUSDT` with real OHLC data and a live "Market open" status; SHIB resolves to `KUCOIN:SHIBUSDT` (was `KUCOIN:SHIBA INUUSDT`, invalid) and its price now reads `$0.0000059`; zKML (confirmed via TradingView's own symbol-search API to have zero real listings anywhere) now shows the clean "no chart" placeholder instead of an error card.

**Known external limitation, not a bug in this app**: while testing, TradingView's own widgetembed occasionally returned empty OHLC (`∅`) even for a confirmed-valid symbol in a fresh, non-embedded tab — this is transient flakiness/rate-limiting on TradingView's side from rapid repeated test requests, unrelated to the code above.

### 🔧 Follow-up: chart clock set to Kuala Lumpur time (2026-09-26 session, same day)
Per explicit request, every coin page's TradingView chart now displays its own bottom-right clock in Kuala Lumpur time instead of TradingView's UTC default. Single-line change: `CoinState._tradingview_iframe_src`'s widget `params` now includes `"timezone": "Asia/Kuala_Lumpur"` (Singapore, `"Asia/Singapore"`, is the noted fallback — same UTC+8 offset — if Kuala Lumpur is ever dropped from TradingView's supported IANA list). Since both light/dark iframe src vars share this one function, this applies to every `/coin/[symbol]` page with zero per-page changes. Verified live on `/coin/btw`: clock read `"04:51:55 UTC+8"`.

Also created `.vscode/mcp.json` this session (Notion MCP server config, `https://mcp.notion.com/mcp`) at the user's request — **not yet authorized**: VS Code's MCP OAuth flow is interactive and was left for the user to complete (Command Palette → **MCP: List Servers** → start `notion` → approve in browser). Unrelated to Feature 2 functionality, just sitting in the repo as workspace config.

### 🔧 Bug fix: TradingView chart missing for DEX-only coins (2026-09-26 session, later same day)
User-reported (with a screenshot of TradingView's own symbol-search UI): `/coin/zkml` (and similar DEX-only coins) showed "No live chart available" even though the user could find a real, working chart by manually searching the bare ticker on tradingview.com, filtering to "Crypto", and picking the right result from the list.

**Root cause, confirmed via TradingView's own public symbol-search API** (`https://symbol-search.tradingview.com/symbol_search/v3/` — needs a browser-like User-Agent/Referer/Origin or it 403s): zKML has zero real CEX listings anywhere (confirmed against CoinGecko's own tickers too — only a Uniswap v2 DEX pool), but TradingView *does* track a real, chartable symbol for that DEX pool directly, under a pool-specific format this app had no way to construct itself (`"ZKMLWETH_315ED6"` — the `315ED6` suffix is a hash TradingView derives from the pool's own contract address, not data CoinGecko's ticker API exposes). The previous session's fix correctly resolves real CEX pairs but — by design at the time — never even attempted DEX pairs, so a genuinely-chartable DEX-only coin like this fell all the way through to the "no chart" placeholder instead. (Separately verified live that ZCAT, which the user also flagged, actually already works correctly — it has real MEXC/Gate CEX pairs and resolves via the existing CEX path with no changes needed; likely just hadn't been visited since server restart when the user checked it.)

**Fix — new last-resort fallback, only ever invoked for a coin with zero real CEX pairs:**
1. New `app/services/tradingview_symbol_service.py`: queries TradingView's own symbol-search API for the coin's ticker (server-side, with the browser-like headers above), filters to real `type == "spot"` results whose de-tagged symbol starts with the ticker, and prefers whichever result's description actually mentions the coin's own name (guards against ticker-collision false matches, same spirit as the previous session's SHIB/"SHIBA INU" fix) before falling back to TradingView's own top relevance match. Skips the external call entirely (no cache write either) whenever `market_pairs_service.get_market_pairs` already found a real CEX pair — this only ever does real work for the CEX-empty case.
2. New Postgres columns (`app/models/coin.py`, migration `70a7887a42ae`): `tradingview_dex_symbol`, `tradingview_dex_symbol_checked_at` — same TTL-cached-column pattern as `cached_market_pairs`/`market_pairs_updated_at`, gated by a new `settings.tradingview_symbol_ttl_hours` (24h default — far longer than market pairs' 1h, since whether a DEX pool has a chartable TradingView symbol at all almost never changes day to day).
3. **Also required a matching change to the *separate* Reflex-side SQLite mirror model** (`frontend/frontend/models/coin.py` — a different `Coin` class from the Postgres one, confirmed live this project maintains both) plus its own Alembic migration (`frontend/alembic/versions/107eaa35737d...`, applied via `reflex db makemigrations` + `reflex db migrate`, NOT the root `alembic` used for Postgres) — missing this the first time round produced a real `ValueError: "Coin" object has no field "tradingview_dex_symbol"` when the background handler tried to write the mirrored row. Both migrations are needed for any future column added to `Coin`, not just this one.
4. `coin_state.py`: new `_fetch_tradingview_dex_symbol`/`CoinState.refresh_tradingview_dex_symbol` (mirrors the existing `_fetch_market_pairs`/`refresh_market_pairs` pattern exactly), registered in `frontend.py`'s `/coin/[symbol]` on_load list. `_resolve_tradingview_symbol` now falls back to the cached `tradingview_dex_symbol` when the CEX-pairs path returns nothing — still a fast, purely-local read (no network call inside the cached var itself, consistent with the previous session's iframe-reload fix). `tradingview_chart_pending` now also waits on a new `tradingview_dex_symbol_fetched` flag, but only when `has_tradingview_chart` is already False (a coin with a real CEX pair never waits on the DEX lookup it never even triggers).

**Verified live (Playwright, real end-to-end page visits through the search UI, not just unit-testing the resolver)**: `/coin/zkml`'s chart iframe now loads `symbol=UNISWAP:ZKMLWETH_315ED6` — a real chart, confirmed independently valid via TradingView's own search API filtered to that exact symbol/exchange. `/coin/zcat` still correctly resolves to `MEXC:ZCATUSDT` via the existing (unmodified) CEX path.

### 🔧 Current State
- **Reflex server IS running** in prod mode (`reflex run --env prod --single-port`) on port 3000 — restarted this session to pick up the DEX-symbol fix above; confirmed responding (`curl localhost:3000` → 200) as of the end of this session.
- **Everything from this session is committed and pushed to `origin/main`** (plain `git`) — working tree is clean as of the last commit (see git log for the exact hash — this fix's commit follows `db60170` in the log).
- **Two migrations applied to local dev, both already run this session**:
  - Root Alembic (`alembic upgrade head`, Postgres) — `70a7887a42ae` (`tradingview_dex_symbol`, `tradingview_dex_symbol_checked_at` on `app/models/coin.py`'s `Coin`).
  - Reflex's own Alembic (`cd frontend && reflex db migrate`, the SQLite mirror) — `107eaa35737d` (same two columns on `frontend/frontend/models/coin.py`'s separate `Coin` class).
  - **Not yet confirmed applied to any other environment** — if this is deployed anywhere besides this dev machine, run both migration commands there before the app boots against that DB/mirror.
- Notion MCP (`.vscode/mcp.json`) still not authorized — unrelated to this fix, carried over from the previous session unchanged.

### 🔧 Bug fix + feature: DEX chart currency mismatch, and Markets pagination (2026-09-26 session, later same day)
User-reported (with a DOM-selector capture of the page's own price header): zKML's chart — just fixed a few hours earlier in this same session — showed a price that didn't match this page's own `$0.00757401` header figure. Also requested: paginate the Markets section's CEX/DEX tables to 10 rows per page each (previously showed all 15 at once).

**Root cause of the price mismatch**: `tradingview_symbol_service._pick_best_match` picked whichever real spot match TradingView's search API happened to list first — for a DEX pool, that's usually the pair quoted in its *actual* trading-pair currency (e.g. `"ZKMLWETH_315ED6"`, priced in WETH), not the `.USD`-suffixed variant TradingView also lists alongside it (`"ZKMLWETH_315ED6.USD"`, TradingView's own USD-converted view of the same pool). The chart itself wasn't showing a *wrong* coin — it was showing the *right* coin's *right* pool, just denominated in WETH instead of USD, which reads as "the price is wrong" next to this page's USD-denominated header.

**Fix**: `_pick_best_match` now ranks candidates by two independent signals — name-match (unchanged from before) and `currency_code == "USD"` — trying name-match+USD first, then name-match alone, then USD alone, then top relevance, rather than blindly taking the first result. ZKML's own already-cached (now-stale) resolution was reset (`tradingview_dex_symbol_checked_at = NULL`, both Postgres and the Reflex SQLite mirror) so it re-resolves under the corrected logic rather than waiting out the 24h TTL. Checked the full `coins` table for any other coin with a cached `tradingview_dex_symbol` at the time of this fix — only zKML had one so far (this whole fallback path only shipped a few hours earlier this same session), so no other coin needed a reset; any coin resolved *after* this fix picks correctly the first time.

**Markets pagination** (`coin_state.py`, `coin_detail.py`): new `CoinState.market_pairs_page` (reset to 1 on both `load_coins` and every CEX/DEX tab switch), `market_pairs_total_pages` (`ceil(len(filtered_market_pairs) / 10)`), and `paged_market_pairs` (the current page's 10-row slice, with a `"rank"` field baked in so the `#` column keeps counting up across pages — e.g. page 2 starts at #11 — instead of restarting at #1). New `market_pairs_prev_page`/`market_pairs_next_page` events, and a `_market_pairs_pagination()` component (reusing the existing `.page-arrow-btn` CSS class from the homepage table's own pagination for visual consistency) that only renders when there's more than one page — a coin with ≤10 real listings on a side shows no pagination controls at all.

**Verified live (Playwright, real end-to-end page visits)**: `/coin/zkml`'s chart now loads `symbol=UNISWAP:ZKMLWETH_315ED6.USD`, matching the page's own `$0.00757401` header. Ethereum's CEX Markets tab: "Page 1 of 2", exactly 10 rows, clicking next correctly advances to "Page 2 of 2" with 5 remaining rows starting at `#11`. Fartcoin's DEX tab independently shows its own "Page 1 of 2" with 10 rows — confirms CEX and DEX paginate independently, not sharing one page counter.

### 🔧 Bug fix: $0.00 price display, and wrong-coin TradingView charts from ticker collisions (2026-09-26 session, later same day)
User-reported (with a DOM-selector capture of CatCoin's price header showing `$0.00`): several coins still showed a flat `$0.00` price despite the previous session's `_fmt_usd` sub-$1-decimal fix. Also reported (with a description of manually re-deriving MUMU's real chart via TradingView's own UI): the resolved chart symbol for some coins was for the *wrong* project entirely.

**Root cause #1 (price)**: NOT a formatting bug — `_fmt_usd` already handles arbitrarily small nonzero prices correctly (verified: SHIB's `$0.00002403` displays fine). CatCoin's own canonical `price_usd` from CMC is *literally* `0` in Postgres (confirmed: `Decimal('0E-8')`) — CMC's own tracked market for this specific ticker reports a flat zero (likely an illiquid/stale market on CMC's side), even though a real, actively-traded pool exists with a genuine nonzero price ($0.00316072 on PumpSwap, already sitting in this coin's own `cached_market_pairs` from CoinGecko). Audited the full `coins` table: **416 coins** currently have `price_usd == 0` in this dev DB, though only ones that have actually been visited (and so have `cached_market_pairs`) can be helped by this fix — the rest have no better data anywhere to fall back to.

**Fix #1**: new `_price_display_with_fallback(price_usd, cached_market_pairs)` in `coin_state.py` — falls back to the coin's own already-fetched top-volume real market pair's price (same CoinGecko-sourced data this page's own Markets section already shows) whenever the canonical CMC price is exactly zero/missing. Only affects the *displayed string* — `price_usd`/`price_raw` (used for market cap, sorting, everything else) are untouched, same "raw stays raw, formatting happens here" division of labor as every other `_fmt_*` helper. Wired into both `_build_row` (every full row rebuild) and `refresh_market_pairs`'s patch (so a coin's *first-ever* visit updates its price the instant real pairs arrive, without waiting for the next 60s sync tick).

**Root cause #2 (wrong chart)**: confirmed live via TradingView's own search API that the exact ticker "MUMU" (and name "Mumu the Bull") is used by **three completely unrelated real coins** on three different chains (Robinhood Chain, Solana, Ethereum) — same collision pattern for "Catcoin" (Ethereum, BNB Chain, Solana, all real, all different projects). `_pick_best_match`'s previous name+USD-only ranking had no way to tell these apart, and separately was also picking TradingView's own **synthetic/discontinued aggregate index** symbols (e.g. `CRYPTO:MUMUTHUSD`, literally flagged `"discontinued"` by TradingView itself) ahead of real per-venue listings.

**Fix #2** (`tradingview_symbol_service.py`): (a) excludes `synthetic`/`index`/`discontinued` typespecs outright — these were never real, chartable venues to begin with. (b) New chain-match signal: reads this coin's own `CoinContract.platform_name` (e.g. "Robinhood Chain" → hint token "robinhood"), and requires at least one real candidate to mention that chain (TradingView's own DEX exchange labels consistently spell it out, e.g. "Uniswap v2 Ethereum", "Raydium Solana") — **when a chain hint exists and no candidate confirms it, returns `None` rather than guessing between same-named-but-unrelated coins**, i.e. this literally implements "check which platform the coin is actually on, and require the resolved TradingView listing to be on that same platform" per explicit request. Coins with only one plausible chain (no collision) are unaffected — verified zKML still resolves to `UNISWAP:ZKMLWETH_315ED6.USD` with this change in place.

**Known, accepted limitation**: this chain-match requirement still requires the ticker-prefix check (`symbol.startswith(ticker)`) to pass first, and some real per-chain listings use a shorter on-chain ticker than CMC's own (possibly disambiguated) symbol — e.g. CatCoin's real Solana pool is listed on TradingView as `CATSOL_...`, which doesn't start with our full ticker `"CATCOIN"`. This coin now correctly shows "No live chart available" (safe, honest) rather than the old wrong synthetic chart — a real net improvement, even though a genuinely-correct chart theoretically exists and isn't found. Loosening the ticker-prefix check further to catch this would reopen the exact "picks an unrelated coin" risk this whole fix exists to close, so it's left as a known gap rather than a regression to chase further this session.

**Audited and reset** every coin with a cached `tradingview_dex_symbol` at the time of these fixes (only MUMU/cmc_id 40960 and CatCoin had one, both from this session's own earlier testing) so they re-resolve under the corrected logic rather than waiting out the 24h TTL.

**Verified live (Playwright, real end-to-end page visits)**: CatCoin's price now reads `$0.00316072` (was `$0.00`) on both the homepage table and its own detail page. `/coin/mumu` now shows "No live chart available" (was the wrong `RAYDIUM:MUMUSOL_FVMZRD.USD` chart, which is real but belongs to a *different* Solana-based "Mumu the Bull" project, not this Robinhood-Chain one). SHIB unaffected: `$0.00000593`, `KUCOIN:SHIBUSDT`. zKML unaffected: `UNISWAP:ZKMLWETH_315ED6.USD`.

**Also answered, no code change**: yes, the TradingView chart is genuinely real-time/live — `widgetembed` is the same live-streaming chart technology tradingview.com's own site uses (not a static image or something this app polls/caches), so it updates independently of this app's own 60s `detail_sync_loop`. Any perceived "lag" would be TradingView's own data-provider licensing for that specific exchange (rare for crypto on their free tier, more common for traditional markets), not something this app controls.

### 🔧 Performance fix: site-wide slowness — SPA routing was not the cause (2026-09-26 session, later same day)
User-reported: clicking buttons, switching pages, and searching all feel slow to finish. Asked whether this is because the app is a single-page app, and requested switching to full browser-reload navigation instead.

**Confirmed by direct timing: it was NOT an SPA-routing problem, and switching to full page reloads would not have helped** (would likely have made it worse — the same expensive work below would still re-run on every fresh page load, plus a full asset re-download and websocket reconnect on top). Reflex's `on_load`/state model doesn't distinguish client-side vs. full-reload navigation for this cost — every `/coin/[symbol]` or `/` visit runs the exact same `on_load` events either way. User confirmed proceeding with the real fix instead of the requested (but wrong-diagnosis) full-reload change.

**Two real, measured causes found, both now fixed:**
1. **`CoinState.load_coins` re-fetched and rebuilt the entire ~8,193-coin universe (eager-loading categories + contracts for every single one) on *every* navigation** — home→coin, coin→home, and coin→coin all share this one `on_load` event. Directly timed: **~2.1 seconds of blocking server time** per call. **Fix**: `load_coins` now skips this entire rebuild whenever `self.all_coins` is already populated (i.e., after the first page visit in a session) — per-page resets (About-section collapse, Markets tab/page) still always run. A brand-new browser session still pays the cost once; every navigation after that is instant.
2. **The bigger cause**: `CoinState.all_coins` — the single state var holding all ~8,193 coins — was **wholly reassigned** by nearly a dozen different handlers (`sync_visible_page` after every category/chain/search/pagination click, `detail_sync_loop`'s 60s tick, `live_sync_loop`'s 60s tick, and all five of a coin page's one-shot background refreshers: social posts, description, business summary, market pairs, TradingView symbol). Directly measured: **the serialized `all_coins` payload is ~31.8 MB**. Every one of those reassignments — even when only 1–100 coins actually changed — re-serialized and re-sent this entire ~32 MB blob over the websocket, and the client had to re-parse and re-render it on every single interaction. This was almost certainly the dominant cost behind "every click feels slow," an order of magnitude bigger than either the artificial 250ms skeleton-visibility delays (`asyncio.sleep(0.25)`, one in each of `set_category`/`set_chain`/`toggle_search`/`set_search_query`/`next_page`/`prev_page`/`go_to_page`/`set_page_size` — an earlier session's deliberate choice, left as-is this session) or the ~1.2s live CMC re-sync `sync_visible_page` does for a 100-coin page.

**Fix #2, applied** (`coin_state.py`): new `CoinState.coin_overrides: dict[int, dict]` — a small dict keyed by `cmc_id`, holding *only* whichever fields a background refresh actually touched for that one coin. `all_coins` itself is now written to exactly once per session (`load_coins`'s initial full load) and never reassigned again — every previously-reassigning handler (`_apply_updates`, `refresh_social_posts`, `refresh_coin_description`, `refresh_business_summary`, `refresh_market_pairs`, `refresh_tradingview_dex_symbol`) now merges its patch into `coin_overrides` instead, which stays orders of magnitude smaller (bounded by however many distinct coins a session has actually touched, not the full universe). New `_row_with_overrides(row)`/`_merged_row(cmc_id)` helpers do the read-side merge — `filtered_coins` (and therefore `paged_coins`/`sorted_paged_coins`, everything the homepage table renders), `selected_coin` (everything a coin's own page renders), and `live_sync_loop`'s top-100 selection all merge overrides in *before* filtering/sorting/paginating, so live-synced data still shows up correctly and immediately — only the *output* of those computed vars (small, already paginated/selected) crosses the websocket now, never the full 8,193-row list again after the first load.

**Verified live (Playwright, real end-to-end page visits, after this fix)**: homepage pagination still correctly advances (page 2 → ranks 101+), search still correctly ranks results by true global market-cap position (Dogecoin → rank 12 among 8 "dogecoin"-named results), and Ethereum's coin-detail page still shows its real price, real chart (`BINANCE:ETHUSDT`), and its Markets/AI-Summarizations/About sections — confirming the `coin_overrides` merge didn't regress any of the business-summary/market-pairs/chart-symbol machinery built earlier this session.

### 🔧 Feature: swapped X-post scraper to a specific Apify actor, 20 posts/profile (2026-09-26 session, later same day)
Per explicit request: switch `social_service.py`'s X-post scraping to a specific Apify actor (`https://console.apify.com/actors/Y3cgyqvI46p0VMr0m` — "Twitter (X) Scraper ✅ No Login or Cookies", `atomus/twitter-scraper`), scraping 20 latest posts per coin's own X profile. The "only call when a coin page is opened" requirement was already true of the existing architecture (`CoinState.refresh_social_posts`, a background `on_load` event on `/coin/[symbol]`, gated by `settings.social_cache_ttl_hours` — unchanged this session) — this was purely a scraper-backend swap, not a new trigger to build.

**Real input/output schema, confirmed live via direct Apify API calls (not guessed from docs)** — this actor's schema is genuinely different from the previously-configured one (`61RPP7dywgiy0JPD0`), so every field name below was verified against real responses before writing the normalizer:
- Request body for "scrape a profile's timeline" mode: `{"searchType": "user-tweets", "handles": [username], "maxItems": 20}` (not the old `{"twitterHandles": [...], "sort": "Latest"}` shape).
- Response is a flat list where **one item is a `{"record_type": "profile"}` summary row** (bio, follower count — not a post) and the rest are `{"record_type": "tweet"}` rows — `social_service.py` now filters to `record_type == "tweet"` before normalizing.
- Tweet fields: `text`, `url`, `created_at` (Twitter's classic date-string format, e.g. `"Fri Sep 25 17:57:31 +0000 2026"` — carried through as `time_display` as-is; confirmed this field isn't actually rendered anywhere in `coin_detail.py`'s post cards today, so no reformatting needed), `favorite_count`/`reply_count`/`retweet_count`, and `media` — a list of `{"type", "image_url", "video_url"}` dicts (confirmed live for a real image post) — different from the old actor's flat-list-of-URL-strings shape.
- A nonexistent handle returns an empty list (confirmed live) — no special "no results" placeholder to filter for, unlike the previously-configured actor.
- **Real run-level failure shape, also confirmed live and now handled explicitly**: `[{"status": "error", "error_kind": "...", "reason": "..."}]` — logged with the actor's own reason text (distinct from an ordinary empty-results no-op) since this is an account/billing-level failure, not a per-coin one.

**Changes**: `.env`'s `APIFY_ACTOR_ID` → `Y3cgyqvI46p0VMr0m`; `app/core/config.py`'s `apify_actor_id` default updated to match, plus new `settings.social_max_posts = 20` (both the scrape request's `maxItems` and the post-normalization slice now read this one setting, not a hardcoded `10`); `app/services/social_service.py`'s `_fetch_from_scraper`/`_normalize` rewritten against the real schema above.

**⚠️ Real, current blocker found while verifying live — flagged to the user, not silently worked around**: this Apify account is on that actor's **free tier**, which caps at **10 free "tweet-scraped" events per calendar month** total (confirmed live via the actor's own error message: *"You have used this month's free 'tweet-scraped' quota (10)... Upgrade to any paid Apify plan to remove the cap"*). Verification testing this session (a few small calls to confirm the schema, then one real end-to-end `SocialService.get_tweets` call for Ethereum) already exhausted it — Ethereum's page got cached with only 3 real tweets before the quota ran out mid-verification, and the account will return that same `{"status": "error", ...}` response for every further scrape attempt (any coin, not just ETH) until either the calendar month rolls over or the Apify account is upgraded to a paid plan. The code handles this gracefully (logs the reason, returns `None`, falls back to whatever's cached or the "View live posts" link — never crashes), but **at 20 posts/coin and a 10-event/month free cap, this feature cannot practically scrape even one full coin's 20-post timeline right now** — this needs the user's Apify account to be on a paid plan (pay-per-event, ~$0.002–0.004/tweet by volume tier per the actor's own pricing) before this is usable beyond the occasional 1–2-coin trickle the free tier allows.

**Verified live**: real end-to-end `SocialService.get_tweets` call for Ethereum returned 3 real, correctly-normalized tweets (real RT text, real engagement counts, real permalink URLs) before hitting the free-tier cap mid-test; `reflex compile --dry` succeeds; server restarted with the new config, responds 200.

### ➡️ Next Steps
1. **Decide on Apify billing** for the X-scraper actor above — the free tier (10 tweet-scrapes/month total, account-wide) is already exhausted this month; scraping even one coin's real 20-post timeline needs a paid Apify plan. Until then, only a small trickle of scrapes will succeed before every further call short-circuits to the graceful-fallback path.
2. Consider reducing/removing the artificial `asyncio.sleep(0.25)` skeleton-visibility delays (8 occurrences) now that the two bigger causes above are fixed — they were an earlier session's deliberate choice to make the skeleton loading state perceptible; a shorter delay (or none) is a much smaller, low-risk change, left as-is this session pending a decision on whether the skeleton flash is still wanted.
3. **Complete Notion MCP authorization**, if Notion access is actually wanted for this project: open the Command Palette → **MCP: List Servers** → start `notion` → approve the OAuth prompt in the browser for the workspace containing "Crypto Intelligence Platform". Until this is done, `.vscode/mcp.json` is inert config with no effect.
4. **Feature 2 architecture decision still open** (carried over across multiple sessions now, still not touched): Feature 2 has real, verified functionality but does NOT yet match `ai-instructions.md`'s literal spec on two points — no scheduled background worker (everything is on-demand/page-view-triggered), and no side-drawer/modal (built inline into the existing page instead). Still needs a decision: (a) accept the inline/on-demand architecture as the real design and update `ai-instructions.md`'s spec to match reality, or (b) build the literal spec's missing pieces (see the `run_business_summary_batch_sync` sketch a few sections up, still unimplemented).
5. Once 4 is resolved (or explicitly deferred), update `ai-instructions.md`'s Feature 2 STATUS from `🔒 LOCKED` to `⏳ IN PROGRESS` so both files agree.
6. Consider applying the same "never guess a bare exchange symbol" pattern to any other place in this codebase that might construct a TradingView/exchange symbol from raw ticker data.
7. If a coin ever turns out to have a real listing TradingView's search API *also* can't find (extremely obscure/new tokens), the chart will correctly show "No live chart available" rather than guessing — this is by design, not a gap to fix.
8. The CatCoin ticker-prefix limitation noted above (short on-chain ticker vs. CMC's own possibly-disambiguated symbol) is a real, known gap — worth revisiting if wrong-ticker-prefix "no chart" cases turn out to be common enough to matter, ideally by requiring name+chain match together as an alternative path rather than loosening the prefix check globally.
9. If `reflex_cache_service.py`'s own nightly Postgres→Reflex-SQLite mirror sync is ever extended to write per-coin fields more granularly, keep in mind `coin_overrides` (in-memory, per Reflex session) and the SQLite mirror (on-disk, shared across sessions) are two separate caches — the mirror write already happens inside each `_fetch_*`/`_sync_and_rebuild_rows` helper, unchanged by this session's `coin_overrides` refactor, which only affects in-memory state shape.
10. The 416-coins-with-`price_usd==0` count (this dev DB) is mostly coins that have never been visited (no `cached_market_pairs` to fall back to) — no further action needed unless that number turns out to matter for coins actually getting traffic; the fallback already covers every visited coin going forward.


